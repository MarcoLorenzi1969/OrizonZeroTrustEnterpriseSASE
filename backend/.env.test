# Orizon Zero Trust - Test Environment Variables
# DO NOT use in production!

# Security
SECRET_KEY=test-secret-key-for-testing-only-not-for-production-use-12345678
JWT_SECRET_KEY=test-jwt-secret-key-for-testing-only-not-for-production-use-12345678
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Database (not used in tests - we use in-memory SQLite)
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=test
POSTGRES_PASSWORD=test
POSTGRES_DB=test_db

# Redis (not used in tests)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# MongoDB (not used in tests)
MONGODB_URL=mongodb://localhost:27017
MONGODB_DB=test_db

# Environment
ENVIRONMENT=test
DEBUG=True
