# Orizon Zero Trust Connect - Production Environment

# API Configuration (relative path - Nginx proxies to backend)
VITE_API_BASE_URL=/api/v1

# WebSocket Configuration (optional - defaults to current host)
# VITE_WS_BASE_URL=wss://your-domain.com

# Hub Configuration (optional - defaults to current hostname)
# VITE_HUB_HOST=your-hub-host.com
